import { KendoGridColumnDateFormatDirective } from './kendo-grid-column-date-format.directive';

describe('KendoGridColumnDateFormatDirective', () => {
    it('should create an instance', () => {
        const directive = new KendoGridColumnDateFormatDirective(null);
        expect(directive).toBeTruthy();
    });
});
